"use client";
import { useState } from "react";
import { ArrowRight, Mail } from "lucide-react";
export default function NewsletterSection(){const [email,setEmail]=useState("");const [done,setDone]=useState(false);function submit(e:React.FormEvent){e.preventDefault();if(!email.trim())return;setDone(true);setEmail("");}return <section className="px-5 py-16 lg:px-10"><div className="mx-auto max-w-7xl overflow-hidden rounded-3xl bg-[#111827] px-6 py-12 text-white sm:px-10 lg:flex lg:items-center lg:justify-between lg:px-14"><div><p className="text-xs font-semibold uppercase tracking-[.25em] text-[#d8b75b]">Private Collection Notes</p><h2 className="mt-3 text-3xl font-semibold sm:text-4xl">A little luxury, delivered to your inbox.</h2><p className="mt-3 max-w-xl text-sm leading-7 text-white/60">Be the first to know about new collections, festive edits and exclusive offers.</p></div><form onSubmit={submit} className="mt-7 flex max-w-lg rounded-full border border-white/15 bg-white/5 p-1 lg:mt-0"><div className="flex min-w-0 flex-1 items-center gap-2 px-4"><Mail size={17} className="text-white/50"/><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="Your email address" className="min-w-0 flex-1 bg-transparent text-sm outline-none placeholder:text-white/40"/></div><button type="submit" className="flex items-center gap-2 rounded-full bg-[#c9a227] px-5 py-3 text-xs font-semibold text-[#111827]">{done?'Subscribed':'Subscribe'}{!done&&<ArrowRight size={14}/>}</button></form></div></section>}
