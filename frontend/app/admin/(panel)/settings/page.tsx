"use client";
import { useEffect, useState } from "react";
import { apiFetch } from "@/lib/api";
export default function SettingsPage(){const [shipping,setShipping]=useState<any>(null);useEffect(()=>{apiFetch('/admin/shipping-settings').then(r=>r.json()).then(j=>setShipping(j.data)).catch(()=>null)},[]);return <div><p className="text-xs font-semibold uppercase tracking-[.2em] text-[#c9a227]">Store Control</p><h1 className="mt-2 text-3xl font-semibold">Settings</h1><div className="mt-7 grid gap-6 lg:grid-cols-2"><section className="rounded-2xl border border-gray-200 bg-white p-6"><h2 className="font-semibold">Store Information</h2><div className="mt-5 space-y-4 text-sm"><div><p className="text-xs text-gray-400">Store</p><p className="mt-1 font-medium">BanglesMart</p></div><div><p className="text-xs text-gray-400">Support email</p><p className="mt-1 font-medium">support@banglesmart.com</p></div><div><p className="text-xs text-gray-400">Currency</p><p className="mt-1 font-medium">INR (₹)</p></div></div></section><section className="rounded-2xl border border-gray-200 bg-white p-6"><h2 className="font-semibold">Shipping</h2>{shipping?<div className="mt-5 space-y-3 text-sm"><div className="flex justify-between"><span className="text-gray-500">Enabled</span><span>{shipping.shipping_enabled ? 'Yes':'No'}</span></div><div className="flex justify-between"><span className="text-gray-500">Flat charge</span><span>₹{Number(shipping.flat_shipping_amount||0).toFixed(2)}</span></div><div className="flex justify-between"><span className="text-gray-500">Free shipping minimum</span><span>{shipping.free_shipping_minimum ? `₹${Number(shipping.free_shipping_minimum).toFixed(2)}` : 'Not set'}</span></div></div>:<p className="mt-5 text-sm text-gray-500">Loading shipping settings...</p>}</section></div></div>}
